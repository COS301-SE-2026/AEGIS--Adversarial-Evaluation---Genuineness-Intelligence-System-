services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: aegis_admin
      POSTGRES_PASSWORD: password
      POSTGRES_DB: aegis_db
    ports:
      - "5432:5432"