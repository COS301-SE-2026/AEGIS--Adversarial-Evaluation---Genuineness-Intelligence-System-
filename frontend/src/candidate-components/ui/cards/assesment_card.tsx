import { AssessmentCardProps } from "./assessment_card.types";

export function AssessmentCard({ assessmentId, title, description, attempts, success_rate }: AssessmentCardProps) {
    return (
        <div>
            
        </div>
    );
}